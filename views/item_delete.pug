extends layout 

block content 

    h1.title= title 

    p Are you sure you want to delete this product?

    form(method='POST')
        input(type='hidden' name='itemid' value=itemInfo._id)

        button(type="submit") Delete