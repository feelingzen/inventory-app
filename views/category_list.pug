extends layout

block content 
    h1.title.list= title 
    ul 
        each category in the_categories 
            li
                a(href=category.url) #{category.name}
                br
                em #{category.description}
                
        else 
            p There are no categories